
export default async function validateUserRutExists(documentType, rut) {
    console.log(documentType, rut)
    if (documentType !== 'rut') return false;
    const rutFound = await
        console.log()
    return !!rutFound;
}
